-- This script computes the average score of all records in the table second_table
-- The result column name will be 'average'

-- Select the average score from the table second_table
SELECT AVG(score) AS average
FROM second_table;
